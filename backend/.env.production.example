# =============================================================================
# Backend Production Environment Configuration Template
# =============================================================================
# Copy this to backend/.env.production on EC2 and update with actual values.
# This file should NEVER be committed to git.
# =============================================================================

# Application Settings
ENVIRONMENT=production
DEBUG=false

# Database (PostgreSQL on EC2 via platform docker-compose)
# Connection string: postgresql://username:password@postgres-platform:5432/database
DATABASE_URL=postgresql://filterical_prod:CHANGE_ME@postgres-platform:5432/filterical_production

# Security - CRITICAL: Generate strong random keys (DIFFERENT from staging!)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE-ME-use-strong-random-key-minimum-32-chars
JWT_SECRET_KEY=CHANGE-ME-use-strong-random-key-minimum-64-chars

# Admin Credentials
# IMPORTANT: Change via admin panel after first deployment
ADMIN_PASSWORD=temporary-change-after-deployment
ADMIN_EMAIL=info@paiss.me

# Backend Configuration
PYTHONPATH=/app
LOG_LEVEL=WARNING
DISABLE_BACKGROUND_TASKS=false
HTTP_TIMEOUT=30
DEV_MODE=false

# SMTP Email Configuration
# Get password from 1Password: "info@paiss.me Mailbox NameCheap"
SMTP_HOST=mail.privateemail.com
SMTP_PORT=587
SMTP_USERNAME=info@paiss.me
SMTP_PASSWORD=CHANGE-ME-get-from-1password
SMTP_FROM_EMAIL=info@paiss.me
