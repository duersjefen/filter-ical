# AWS Configuration - Copy this file to .env and fill in your values
AWS_ACCOUNT_ID=your-account-id-here
AWS_REGION=eu-north-1
AWS_ACCESS_KEY_ID=your-access-key-id-here
AWS_SECRET_ACCESS_KEY=your-secret-access-key-here

# ECR Repository URIs (replace with your account ID)
ECR_BACKEND_URI=your-account-id.dkr.ecr.eu-north-1.amazonaws.com/ical-viewer-backend
ECR_FRONTEND_URI=your-account-id.dkr.ecr.eu-north-1.amazonaws.com/ical-viewer-frontend

# EC2 Configuration (update these when you create EC2 instance)
EC2_HOST=your-ec2-public-ip-here
EC2_SSH_KEY_PATH=path/to/your-key.pem

# GitHub Secrets Values (copy these to GitHub manually)
# These are the values you need to set in GitHub Actions secrets:
# EC2_HOST=your-ec2-public-ip (get from EC2 console)
# EC2_USER=ec2-user (default for Amazon Linux)
# EC2_SSH_KEY=content-of-your-pem-file (private key content)

# For OIDC setup (more secure than access keys):
# No GitHub secrets needed for AWS credentials - uses IAM roles with OIDC