# =============================================================================
# Filter-iCal Project Configuration Template
# =============================================================================
# Copy this to .env and customize for your deployment
# This template reflects the GHCR + multi-tenant platform architecture

# Domain Configuration (REQUIRED)
DOMAIN_NAME=your-domain.com

# Deployment Configuration (REQUIRED)
EC2_HOST=your.server.ip.address

# Application Ports (optional - defaults shown)
PORT_BACKEND=3000
PORT_FRONTEND=8000

# Project Name
PROJECT_NAME=filter-ical

# =============================================================================
# SECRETS (NEVER commit these - add to GitHub Secrets instead)
# =============================================================================
# Required GitHub repository secrets:
# - EC2_USER (usually 'ec2-user' or 'ubuntu')
# - EC2_SSH_PRIVATE_KEY (content of your SSH private key)
# - GHCR_TOKEN (GitHub Personal Access Token with packages:read/write)
# - DATABASE_URL_STAGING (PostgreSQL connection string for staging)
# - DATABASE_URL_PRODUCTION (PostgreSQL connection string for production)
# - SECRET_KEY_STAGING (API secret key for staging environment)
# - SECRET_KEY_PRODUCTION (API secret key for production environment)
