# =============================================================================
# Project Configuration Template (Copy to .env and customize)
# =============================================================================
# This file shows what values you need to configure for deployment.
# Copy this to .env and update the values for your project.

# Domain Configuration (REQUIRED)
DOMAIN_NAME=your-domain.com

# AWS Configuration (REQUIRED)
AWS_ACCOUNT_ID=123456789012
AWS_REGION=us-east-1
ECR_REGISTRY=123456789012.dkr.ecr.us-east-1.amazonaws.com

# Deployment Configuration (REQUIRED)
EC2_HOST=your.server.ip.address

# Application Ports (optional - defaults will be used)
PORT_BACKEND=3000
PORT_FRONTEND=8000

# =============================================================================
# SECRETS (NEVER commit these - add to GitHub Secrets instead)
# =============================================================================
# These should be added as GitHub repository secrets, not in .env:
# - AWS_ACCESS_KEY_ID
# - AWS_SECRET_ACCESS_KEY  
# - EC2_USER (usually 'ec2-user' or 'ubuntu')
# - EC2_SSH_KEY (content of your .pem file)
