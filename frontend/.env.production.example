# =============================================================================
# Frontend Production Environment Configuration Template
# =============================================================================
# Copy this to frontend/.env.production on EC2 and update with actual values.
# This file should NEVER be committed to git.
#
# IMPORTANT: Vite variables (VITE_*) are BAKED INTO THE BUILD.
# Changing this file requires rebuilding the frontend container:
#   docker-compose build frontend && docker-compose up -d frontend
# =============================================================================

# Backend API URL (accessed by browser, must be public URL)
VITE_API_BASE_URL=https://filter-ical.de
